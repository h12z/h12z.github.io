name: Deploy
run-name: ${{github.actor}}

on: [push]
jobs:
    Deploy:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
              with:
                path: h12z
